package com.karabelas.kfs.user;

/**
 * Mock service interface. Public — this is the seam other feature
 * packages (knowledgebase, entry, etc.) are allowed to depend on.
 */
public interface UserService {
    // e.g. UserDto findById(Long id);
}
