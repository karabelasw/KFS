package com.karabelas.kfs.entry;

/**
 * Mock service interface. Public — the seam node/tag/relationship
 * packages depend on (e.g. to validate an entryId exists).
 */
public interface EntryService {
}
