package com.karabelas.kfs.node;

/**
 * Mock service interface. Public.
 * e.g. reorderEntries(nodeId, newOrder) — owns Node-scoped ordering
 * logic entirely; entry package never needs to know this exists.
 */
public interface NodeService {
}
